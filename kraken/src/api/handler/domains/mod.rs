//! The handlers and schemas for the aggregated data of domains are located here

#[cfg(feature = "bin")]
pub(crate) mod handler;

/// The schemas for the aggregated data of domains are located here
pub mod schema;
