//! Endpoints for inspecting and revoking a user's oauth decisions

#[cfg(feature = "bin")]
pub(crate) mod handler;

/// Schemas for the endpoints for inspecting and revoking a user's oauth decisions
pub mod schema;
