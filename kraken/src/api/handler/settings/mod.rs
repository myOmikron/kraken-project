//! This module holds the handler to retrieve and update settings

#[cfg(feature = "bin")]
pub(crate) mod handler_admin;

/// This module holds the schemas to retrieve and update settings
pub mod schema;
